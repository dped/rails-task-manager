<h1>Your tasks</h1>

<% link_to "Add a task", tasks_new_path %>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task.id) %></h2>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
  <% link_to "Remove a task", task_path(task), method: :delete %>
<% end%>
